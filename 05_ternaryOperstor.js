var mark=62;
//if mark greater than equal 60 
//then allow for interview else dont alow for interview
var result= mark>=60 ? "Allow for interview" : "Do not allow";
console.log(`Marks is ${mark}==> ${result}`);

// if number is even than return "EVEN" else "ODD"
var result = num%2 == 0 ? "EVEN" : "ODD" ;